'use client';

import { FC } from 'react';
import { EventEditor } from '../../components/sections/EventEditor';

const Page: FC = () => {
  return (
    <div>
      <EventEditor />
    </div>
  );
};

export default Page;
