'use client';

import { FC } from 'react';

interface PageProps {
  searchParams: { 'date-selected': string | undefined };
}

const Page: FC<PageProps> = (props) => {
  return <div></div>;
};

export default Page;
